schema {
	query: Query
}

type Query {
	getBooks: [Book]
	getBook(id: ID) : Book
	getPaginatedElements(first: Int, after: String): BookConnection @connection(for: "Book")
}

type Book {
	id: ID
	title: String
	author: String
}
